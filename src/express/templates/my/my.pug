extends ../layout.pug

block content
  main.main-page.main-page--padding
    section.main-page__notes.notes
      h1.notes__title Мои записи
      ul.notes__list
        each article in articles
          li.notes__list-item(class=(articles[articles.length - 1] == article) ? 'notes__list-item--last' : '')
            time.notes__item-time(datetime='2019-03-21T20:33')=article.createdDate
            a.notes__item-text(href=('/articles/edit/' + article.id))=article.announce
            button.notes__button.button.button--close-item(type='button')
              span.visually-hidden Закрыть строку списка
